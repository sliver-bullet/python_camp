coordinate = (10, 20)
person = ("Alice", 25, "Engineer")
single_item = (42,) # Note the comma for single item

# Tuple operations
print(coordinate[0]) # Accessing elements
print(len(person)) # Length
print(person) 
print(single_item.append(asd)) # Single item tuple
